# 基础对象

提供默认登录对象、玩家对象和临时房间，可直接用于最小宿主，也可继承扩展。新项目可从这些对象起步；已有 MUD 可保留自己的对象，只接入需要的框架能力。

宿主通过 `LOGIN_OB`、`USER_OB`、`VOID_OB` 覆盖默认对象；定制实现继承对应的 `CORE_*_OB`。具体约定见 [接入指南](../../docs/integration.md) 和 [登录说明](../../docs/daemons/login_d.md)。
