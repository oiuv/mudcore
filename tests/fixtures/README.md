# 本机 TLS 测试材料

此目录的私钥是为可复现测试生成、随源码公开分发的测试材料，不是任何部署环境的凭据，禁止用于真实服务。

- `localhost-*`：可信测试证书，名称为 localhost。
- `untrusted-*`：同名但不加入测试信任库，验证证书链拒绝行为。
- `mismatch-*`：加入测试信任库但名称为 wrong.invalid，记录驱动是否实际执行主机名校验。

测试运行器只给临时 driver 子进程设置 `SSL_CERT_FILE`，不修改系统或宿主服务的信任库。证书有效期至 2036 年 9 月，届时需更新这些测试材料。
