#include <tui.h>

inherit TUI_TERMINAL;

private object app, appScreen, appWidget;
private mapping options;

void command(string text);

private void ready() {
    receive("READY\n");
    input_to((: command :));
}

void logon() {
    receive("SUPPORTED:" + tui_supported() + "\n");
    ready();
}

private void line_done(mixed text, int state) {
    receive("LINE:" + json_encode(({ text, state })) + "\n");
    ready();
}

private void selection_done(int index, string item, int state) {
    MASTER_OB->check(options["multi"] == 1, "TUI select does not mutate options");
    receive("SELECT:" + json_encode(({ index, item, state })) + "\n");
    ready();
}

private void multi_done(int *indexes, string *items, int state) {
    receive("MULTI:" + json_encode(({ indexes, items, state })) + "\n");
    ready();
}

private void confirm_done(int yes, int state) {
    receive("CONFIRM:" + json_encode(({ yes, state })) + "\n");
    ready();
}

private void chain_done(int index, string item, int state) {
    tui_readline((: line_done :), ([ "prompt": "CHAIN>" ]));
}

private void recovered() {
    MASTER_OB->check(!tui_active(), "TUI callback failure resets mode");
    ready();
}

private void failure_done(mixed text, int state) {
    call_out((: recovered :), 0);
    error("expected TUI callback failure\n");
}

private void captured(string text) {
    receive("CAPTURE:" + text + "\n");
    ready();
}

private void app_closed() {
    ready();
}

private void try_async_close() {
    mixed err;

    err = catch(tui_close());
    MASTER_OB->check(
        err != 0 && tui_active(),
        "TUI refuses asynchronous close without losing input"
    );
    receive("ASYNC REFUSED\n");
}

private void try_async_app_quit() {
    mixed err;

    err = catch(app->app_quit());
    MASTER_OB->check(err != 0 && tui_active() && app && appScreen && appWidget,
        "TUI refuses asynchronous app exit without destroying its resources");
    receive("ASYNC APP REFUSED\n");
}

void command(string text) {
    mixed err;

    switch (text) {
        case "read":
            tui_readline((: line_done :), ([ "prompt": "INPUT>" ]));
            break;
        case "select":
            options = ([ "multi": 1 ]);
            tui_select((: selection_done :), "SELECT>", ({ "金创药", "干粮", "清水" }), options);
            break;
        case "multi":
            tui_multiselect((: multi_done :), "MULTI>", ({ "a", "b", "c" }));
            break;
        case "async-close":
            tui_confirm((: confirm_done :), "ASYNC>", 1);
            call_out((: try_async_close :), 0);
            break;
        case "confirm":
            tui_confirm((: confirm_done :), "CONFIRM>", 1);
            break;
        case "chain":
            tui_select((: chain_done :), "CHAIN SELECT>", ({ "next" }));
            break;
        case "fail":
            tui_readline((: failure_done :), ([ "prompt": "FAIL>" ]));
            break;
        case "async-app":
        case "app":
            app = new(CORE_DIR "system/object/tui_demo");
            app->set_on_exit((: app_closed :));
            tui_open(app);
            appScreen = app->app_screen();
            appWidget = app->app_widgets()[0];
            if (text == "async-app") call_out((: try_async_app_quit :), 0);
            break;
        case "idle-close":
            input_to((: captured :));
            tui_close();
            receive("CAPTURE>\n");
            break;
        case "busy":
            input_to((: captured :));
            err = catch(tui_confirm((: confirm_done :), "busy"));
            MASTER_OB->check(err != 0 && !tui_active(), "TUI preserves existing input handler");
            receive("CAPTURE>\n");
            break;
        case "status":
            MASTER_OB->check(!app && !appScreen && !appWidget, "TUI application resources freed");
            receive(sprintf("SIZE:%d,%d ACTIVE:%d\n", tui_width(), tui_height(), tui_active()));
            ready();
            break;
        default:
            receive("COMMAND:" + text + "\n");
            ready();
    }
}

void net_dead() {
    if (app) app->set_on_exit(0);
    tui_destroy();
    MASTER_OB->check(!app && !appScreen && !appWidget, "TUI disconnect cleanup");
    destruct(this_object());
}
